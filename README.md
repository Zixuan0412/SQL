The Data Cleaning Projects is an intermidiate level SQL project that generate using AI, the samples are very close to real-world data.

I cleaned up the entire layoffs including the corrupted words on location columns using binary.
